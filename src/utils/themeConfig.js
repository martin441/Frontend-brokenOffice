import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    contrastThreshold: 4.5,
    primary: {
      main: "#BFD732",
    },
    secondary: {
      main: "#FFFFFF",
    },
    third: {
      main: "#444444",
    },
  },
});
